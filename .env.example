# --- Resend (envío de PDF por correo) ---
RESEND_API_KEY=
# Debe ser un remitente de un dominio verificado en Resend, ej. "Power Delivery <check7@powerdelivery.mx>"
RESEND_FROM_EMAIL="Power Delivery <onboarding@resend.dev>"

# --- Monday.com (item por cada servicio) ---
# Board "Check 7 Digital" (id 18431455073). Solo el token es obligatorio —
# el board ID y las columnas ya tienen como default las del tablero real;
# estas variables existen por si el board cambia o hay que apuntar a otro.
MONDAY_API_TOKEN=
MONDAY_BOARD_ID=18431455073
MONDAY_COL_FECHA=date4
MONDAY_COL_ID_CLIENTE=text_mm78scw3
MONDAY_COL_CORREO=email_mm7827d4
MONDAY_COL_PDF=file_mm78g7t1

# --- Solo para desarrollo local (generación de PDF) ---
# Ruta al ejecutable de Chrome en tu Mac, si no está en la ubicación por defecto.
# CHROME_EXECUTABLE_PATH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
